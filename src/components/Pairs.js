import { Typography } from "@material-ui/core";
import React from "react";

import Layout from "./Layout";

export default function Pairs() {
  return (
    <Layout>
      <Typography variant="h2">Pairs</Typography>
    </Layout>
  );
}
