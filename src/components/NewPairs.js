import { Typography } from "@material-ui/core";
import React from "react";

import Layout from "./Layout";

export default function NewPairs() {
  return (
    <Layout>
      <Typography variant="h2">New pairs set</Typography>
    </Layout>
  );
}
